# ==========================================
# Khaled Auto Spares (KAS) - Hosting Config
# ==========================================

# Server Port & Environment
PORT=5000
NODE_ENV=production

# Master Admin Credentials (The ONLY default super admin)
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@kas.dz
ADMIN_PASSWORD=0662136563!
ADMIN_NAME=KAS Administrator

# CORS Configuration (comma separated domains or * for wildcard)
CORS_ORIGIN=https://yourdomain.com,http://localhost:3000

# Database Configuration (leave empty for default SQLite, or provide PostgreSQL URL)
# DATABASE_URL=postgresql://user:password@host:5432/kas_db?sslmode=require
